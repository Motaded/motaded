
    {% if delta == 0 %}
        <div class="tab-pane fade active show" id="Tab-2-{{delta}}" role="tabpanel">
            {# {{ content.field_content.{{delta}} }} #}
            <div class="media-lists">
            {{ content.field_contents }}
            </div>
        </div>
    {% else %}
        <div class="tab-pane fade" id="Tab-2-{{delta}}" role="tabpanel">
            {# {{ content.field_content.{{delta}} }} #}
            <div class="media-lists">
            {{ content.field_contents }}
            </div>
        </div>
    {% endif %}
