<!-- Plans Style 1 Section -->
<section class="pricing-1 dt-section bg-light-gray">
    <div class="pricing-style-1">
        <div class="container">
            {% if content.field_title[0] %}
                <div class="home-1-title">
                    <span><span>{{ content.field_bg_caption.0 }}</span><span>{{ content.field_tag_line.0 }}</span></span>
                    <h2>{{ content.field_title.0 }}</h2>
                </div>
            {% endif %}
            <div class="row">
                {{ content.field_plan_style_1 }}
            </div>
        </div>
    </div>
</section>

