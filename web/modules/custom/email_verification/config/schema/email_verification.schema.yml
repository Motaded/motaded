email_verification.settings:
  type: config_object
  label: 'Email verification settings'
  mapping:
    # Dynamic webform machine names as keys — skip strict typed validation to avoid edge cases.
    webforms:
      type: ignore
      label: 'Webforms that require OTP before submit'
